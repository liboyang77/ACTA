# ACTA
We provide the executable files in this repository. The files are compiled through pipenv.

## Usage
Clone the repository and run framework.exe. The file will load the datasets and output the results of the algorithms. The parameters are  already set in code.

